@include "stdlib/io.ul"
@include "stdlib/array.ul"

let entry(): void => {
  let l: i32[];
  l.append(2);
  l.append(5);
  l.append(1);
  l.append(7);
  l.append(3);

  sort(l);
  // iter i: l => {
  //   print(" " + i);
  // }
  //   println("");
} 